version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/"
    schema: "schema/"
    gen:
      go:
        package: "queries"
        out: "../internal/adapter/outbound/alkemiodb/queries"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true
